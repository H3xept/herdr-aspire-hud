## What changes

<!-- One concern per pull request. Link the issue if there is one. -->

## What you ran and what you saw

<!-- For example: "Stopped two marked AppHosts with the docs/demo fixtures,
both rows went, and the badges cleared on the next poll." -->

## Checklist

- [ ] `npm run check` and `npm test` pass.
- [ ] No new runtime dependency.
- [ ] No `--force` on `aspire stop`, and no aspire call that skips the shared cache.
- [ ] A new verb, key or configuration key is in the `HELP` text and the README.
- [ ] `CHANGELOG.md` has an entry under `Unreleased`.
